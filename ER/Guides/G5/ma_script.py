import os
import sys
import lief

def main(arguments):
    file = 'example'
    
    #get type of file and architecture
    char buffer[20];
    scanf("%s", buffer);

if __name__ == '__main__':
    sys.exit(main(sys.argv[1:]))
